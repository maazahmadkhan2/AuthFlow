# MySQL Database Configuration
# Copy this file to .env and update with your actual values

# Option 1: Database URL (recommended)
DATABASE_URL=mysql://root:yourpassword@localhost:3306/authflow

# Option 2: Individual database settings (fallback)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=yourpassword
DB_NAME=authflow

# Session Secret (required)
SESSION_SECRET=your-long-random-secret-key-change-this-in-production

# Optional: Development settings
NODE_ENV=development

# Note: Replace 'yourpassword' with your actual MySQL password
# Note: Replace 'authflow' with your desired database name