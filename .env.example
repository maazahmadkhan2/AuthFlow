# Firebase Configuration (required for authentication)
VITE_FIREBASE_API_KEY=your-firebase-api-key
VITE_FIREBASE_PROJECT_ID=your-firebase-project-id
VITE_FIREBASE_APP_ID=your-firebase-app-id

# MySQL Database Configuration (optional - fallback to in-memory storage)
DATABASE_URL=mysql://root:yourpassword@localhost:3306/authflow

# Individual database settings (fallback)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=yourpassword
DB_NAME=authflow

# Session Secret (required for backend sessions)
SESSION_SECRET=your-long-random-secret-key-change-this-in-production

# Development settings
NODE_ENV=development

# Instructions:
# 1. Get Firebase keys from Firebase Console (https://console.firebase.google.com)
# 2. Replace 'yourpassword' with your actual MySQL password
# 3. Replace 'authflow' with your desired database name